import { IsEntireRowSelectedPipe } from './is-entire-row-selected.pipe';

describe('IsEntireRowSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsEntireRowSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
