import { IsSelectedPaymentPipe } from './is-selected-payment.pipe';

describe('IsSelectedPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
