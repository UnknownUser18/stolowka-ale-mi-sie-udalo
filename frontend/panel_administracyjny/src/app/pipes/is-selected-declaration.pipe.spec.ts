import { IsSelectedDeclarationPipe } from './is-selected-declaration.pipe';

describe('IsSelectedDeclarationPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedDeclarationPipe();
    expect(pipe).toBeTruthy();
  });
});
