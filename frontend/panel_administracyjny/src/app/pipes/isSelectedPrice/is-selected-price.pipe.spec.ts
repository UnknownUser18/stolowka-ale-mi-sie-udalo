import { IsSelectedPricePipe } from './is-selected-price.pipe';

describe('IsSelectedPricePipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedPricePipe();
    expect(pipe).toBeTruthy();
  });
});
