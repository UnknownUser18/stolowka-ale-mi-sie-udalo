import { DayInMoreDeclarationsPipe } from './day-in-more-declarations.pipe';

describe('DayInMoreDeclarationsPipe', () => {
  it('create an instance', () => {
    const pipe = new DayInMoreDeclarationsPipe();
    expect(pipe).toBeTruthy();
  });
});
