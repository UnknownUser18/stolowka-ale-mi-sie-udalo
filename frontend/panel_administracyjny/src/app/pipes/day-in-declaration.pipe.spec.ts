import { DayInDeclarationPipe } from './day-in-declaration.pipe';

describe('DayInDeclarationPipe', () => {
  it('create an instance', () => {
    const pipe = new DayInDeclarationPipe();
    expect(pipe).toBeTruthy();
  });
});
