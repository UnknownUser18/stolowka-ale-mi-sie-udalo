import { IsAbsencePipe } from './is-absence.pipe';

describe('IsAbsencePipe', () => {
  it('create an instance', () => {
    const pipe = new IsAbsencePipe();
    expect(pipe).toBeTruthy();
  });
});
