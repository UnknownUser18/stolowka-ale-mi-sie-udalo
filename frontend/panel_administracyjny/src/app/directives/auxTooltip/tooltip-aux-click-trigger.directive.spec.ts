import { TooltipAuxClickTriggerDirective } from './tooltip-aux-click-trigger.directive';

describe('TooltipAuxClickTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipAuxClickTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
