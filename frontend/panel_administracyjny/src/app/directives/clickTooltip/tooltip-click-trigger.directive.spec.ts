import { TooltipClickTriggerDirective } from './tooltip-click-trigger.directive';

describe('TooltipClickTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipClickTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
