import { TooltipDelayTriggerDirective } from './tooltip-delay-trigger.directive';

describe('TooltipDelayTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipDelayTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
